# Operating wallet for the workflow (64-hex chars, no 0x prefix).
CRE_ETH_PRIVATE_KEY=YOUR_64_CHARACTER_PRIVATE_KEY_HERE

# Shared secret for the Distro API (sent as x-cre-api-key to GET /cre/batch).
# Must match CRE_API_KEY in the api/ .env.
DISTRO_API_KEY_VAR=YOUR_DISTRO_CRE_API_KEY
