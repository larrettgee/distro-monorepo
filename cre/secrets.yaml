# Maps the secret name used in code (runtime.getSecret("DISTRO_API_KEY")) to the
# env var that supplies its value during simulation. This is the shared secret
# sent to the Distro API's GET /cre/batch (x-cre-api-key header).
secretsNames:
  DISTRO_API_KEY:
    - DISTRO_API_KEY_VAR
