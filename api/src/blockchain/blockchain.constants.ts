/**
 * Sentinel `token` value marking a job funded with the chain's native currency.
 * Mirrors `DistroEscrow.NATIVE` (address(0)). On Arc the native gas token is USDC.
 */
export const NATIVE_TOKEN = '0x0000000000000000000000000000000000000000';
