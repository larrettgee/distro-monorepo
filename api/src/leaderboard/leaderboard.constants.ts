/** Default number of clippers returned by the public leaderboard. */
export const DEFAULT_LEADERBOARD_LIMIT = 50;

/** Hard cap on a single leaderboard page. */
export const MAX_LEADERBOARD_LIMIT = 100;
